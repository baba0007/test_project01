from setuptools import setup, find_packages


# setup(name='package2', version='0.1', packages=[
#      'first_package2', 'second_package2', 'third_package2', 'etc...'])

# or

setup(name='package2', version='0.1', packages=find_packages())
